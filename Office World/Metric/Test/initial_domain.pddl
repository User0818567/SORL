(define
    (domain OfficeWorld)
    (:requirements :typing :fluents)
    (:predicates
        (haveCoffee)
        (haveMail)
        (deliveredCoffee)
        (deliveredMail)
    )
    (:functions (quality))
    